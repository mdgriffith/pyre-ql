# pyre

NPM wrapper package for the Pyre CLI.

This package provides the `pyre` command via `npx pyre` (or global install), and will resolve a platform-specific binary when distribution wiring is complete.

## Usage

```bash
npx pyre --help
```
